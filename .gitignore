node_modules/
.claude/
.wrangler/
backups/
package-lock.json

# Build outputs (regenerables)
styles.css

# Tailwind config local (no del repo)
input.css
tailwind.config.js

# Documentos binarios: por privacidad (staff/) o tamaño (formato MINEDUC)
*.docx

# PDFs grandes de bases curriculares y normativa MINEDUC
# (publicos pero pesados; se descargan desde curriculumnacional.cl)
data/mineduc/bases-curriculares/*.pdf
data/mineduc/normativa/**/*.pdf
data/mineduc/calendario-escolar/**/*.pdf

# Tenants: logos PNG son livianos y se versionan;
# .docx con info de personal se ignoran por privacidad (regla *.docx arriba)
